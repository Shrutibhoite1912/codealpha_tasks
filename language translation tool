<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Translator UI</title>
  <link rel="stylesheet" href="styles.css" />
</head>
<body>
  <main class="container">
    <h1>Translator</h1>

    <div class="row">
      <div class="col">
        <label for="sourceLang">Source language</label>
        <select id="sourceLang">
          <option value="auto">Auto-detect</option>
        </select>
      </div>

      <div class="col">
        <label for="targetLang">Target language</label>
        <select id="targetLang">
        </select>
      </div>
    </div>

    <textarea id="sourceText" placeholder="Enter text to translate..." rows="6"></textarea>

    <div class="actions">
      <button id="translateBtn">Translate</button>
      <select id="provider">
        <option value="libre">LibreTranslate (no key)</option>
        <option value="google">Google Translate (API key required)</option>
        <option value="microsoft">Microsoft Translator (Azure key required)</option>
      </select>
    </div>

    <div class="result">
      <label for="translatedText">Translated text</label>
      <div id="translatedText" class="translated" aria-live="polite"></div>

      <div class="result-actions">
        <button id="copyBtn">Copy</button>
        <button id="speakBtn">Speak</button>
      </div>
    </div>

    <div id="status" class="status"></div>
  </main>

  <script src="script.js"></script>
</body>
</html>
